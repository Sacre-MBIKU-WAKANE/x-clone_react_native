import { View, Text } from "react-native";
import React from "react";

type Props = {};

const grok = (props: Props) => {
	return (
		<View>
			<Text>grok</Text>
		</View>
	);
};

export default grok;
